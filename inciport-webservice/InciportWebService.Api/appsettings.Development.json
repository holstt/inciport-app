{
    "DbInitialization": {
        // Do not recreate the db every time the application is run.
        // Only one of these can be true.

        // Create only the default db that is required for the application to function.
        "ShouldCreateDefaultDb": false,
        // Create default db + initialize with test data
        "ShouldCreateTestDb": true
    },
    "ShouldPublishSwaggerUI": true,
    // Authencication
    "JWT": {
        "ValidAudience": "http://localhost:5000",
        "ValidIssuer": "http://localhost:5000",
        "Secret": "This is the demo secret",
        "TokenExpirationHours": "99999"
    }
}